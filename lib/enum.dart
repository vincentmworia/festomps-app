enum AuthenticationMode {
  login,
  signup,
}

enum Station {
  distribution,
  sorting,
  all,
}
enum Status{
  offline,
online
}

enum Workpiece {
  metallic,
  black,
  red,
  unknown,
}

enum MachineMode {
  auto,
  manual,
}
